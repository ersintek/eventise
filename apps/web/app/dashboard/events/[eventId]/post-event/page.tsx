import { cookies } from 'next/headers';
import { redirect } from 'next/navigation';
import { EventNav } from '../../../../components/navigation';
import { PostEventManager } from './post-event-manager';
export default async function PostEventPage({params}:{params:Promise<{eventId:string}>}){const token=(await cookies()).get('eventise_session')?.value;if(!token)redirect('/login');const api=`${process.env.API_INTERNAL_URL}/api`,headers={authorization:`Bearer ${token}`};const organizationResponse=await fetch(`${api}/organizations`,{headers,cache:'no-store'});if(organizationResponse.status===401)redirect('/login');const organizations=organizationResponse.ok?await organizationResponse.json()as Array<{id:string}>:[];if(!organizations.length)redirect('/onboarding');const{eventId}=await params,organizationId=organizations[0].id;const[summaryResponse,photosResponse]=await Promise.all([fetch(`${api}/organizations/${organizationId}/events/${eventId}/reports/summary`,{headers,cache:'no-store'}),fetch(`${api}/organizations/${organizationId}/events/${eventId}/photos`,{headers,cache:'no-store'})]);return <main className="builder-shell"><header><div><p className="eyebrow">ETKİNLİK SONRASI</p><h1>Sonuçlar ve çıktılar</h1><p>Etkinliğin etkisini görün, içerikleri yönetin ve çıktıları hazırlayın.</p></div></header><EventNav eventId={eventId} active="post"/><PostEventManager organizationId={organizationId} eventId={eventId} initialSummary={summaryResponse.ok?await summaryResponse.json():{registrations:0,accepted:0,checkedIn:0,attendanceRate:0,approvedPhotos:0,feedbackSubmissions:0}} initialPhotos={photosResponse.ok?await photosResponse.json():[]}/></main>}
