import Link from 'next/link';import{cookies}from'next/headers';import{redirect}from'next/navigation';import{PostEventManager}from'./post-event-manager';
export default async function PostEventPage({params}:{params:Promise<{eventId:string}>}){const token=(await cookies()).get('eventise_session')?.value;if(!token)redirect('/login');const organizations=await fetch(`${process.env.API_INTERNAL_URL}/api/organizations`,{headers:{authorization:`Bearer ${token}`},cache:'no-store'}).then(r=>r.json())as Array<{id:string}>;if(!organizations.length)redirect('/onboarding');const{eventId}=await params,organizationId=organizations[0].id,headers={authorization:`Bearer ${token}`};const[summaryResponse,photosResponse]=await Promise.all([fetch(`${process.env.API_INTERNAL_URL}/api/organizations/${organizationId}/events/${eventId}/reports/summary`,{headers,cache:'no-store'}),fetch(`${process.env.API_INTERNAL_URL}/api/organizations/${organizationId}/events/${eventId}/photos`,{headers,cache:'no-store'})]);return <main className="builder-shell"><header><Link href={`/dashboard/events/${eventId}`}>← Başvurular</Link><div><p className="eyebrow">ETKİNLİK SONRASI</p><h1>Arşiv ve çıktılar</h1></div></header><PostEventManager organizationId={organizationId}eventId={eventId}initialSummary={summaryResponse.ok?await summaryResponse.json():{registered:0,checkedIn:0}}initialPhotos={photosResponse.ok?await photosResponse.json():[]}/></main>}
