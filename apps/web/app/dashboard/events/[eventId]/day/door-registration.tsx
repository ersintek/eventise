'use client';import{FormEvent,useState}from'react';export function DoorRegistration({organizationId,eventId}:{organizationId:string;eventId:string}){const[message,setMessage]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget,fd=new FormData(form),r=await fetch(`/api/backend/organizations/${organizationId}/events/${eventId}/check-in/door`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({firstName:fd.get('firstName'),lastName:fd.get('lastName'),email:fd.get('email')})});const data=await r.json();setMessage(r.ok?'Kapıda kayıt ve katılım teyidi oluşturuldu.':(Array.isArray(data.message)?data.message.join(' '):data.message));if(r.ok)form.reset()}return <form className="registration-card door-form"onSubmit={submit}><h2>Kapıda kayıt</h2><div className="two"><label>Ad<input name="firstName"required/></label><label>Soyad<input name="lastName"required/></label></div><label>E-posta<input name="email"type="email"required/></label><button className="primary">Kaydet ve teyit et</button>{message&&<p className="notice">{message}</p>}</form>}
