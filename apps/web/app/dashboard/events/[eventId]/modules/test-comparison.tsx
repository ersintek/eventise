'use client';
type Summary = { submissions: number; average: number|null };
export function TestComparison({comparison}:{comparison:{pre:Summary;post:Summary;improvement:number|null}}){return <article className="workspace-card wide"><h2>Ön test–son test karşılaştırması</h2><div className="metric-grid field-metrics"><div><span>Ön test</span><b>{comparison.pre.average===null?'—':`%${comparison.pre.average}`}</b><small>{comparison.pre.submissions} yanıt</small></div><div><span>Son test</span><b>{comparison.post.average===null?'—':`%${comparison.post.average}`}</b><small>{comparison.post.submissions} yanıt</small></div><div><span>Değişim</span><b>{comparison.improvement===null?'—':`${comparison.improvement>=0?'+':''}${comparison.improvement}`}</b><small>puan</small></div></div></article>}
