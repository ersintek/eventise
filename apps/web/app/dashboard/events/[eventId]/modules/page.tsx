import Link from 'next/link';
import { cookies } from 'next/headers';
import { redirect } from 'next/navigation';
import { ModuleManager } from './module-manager';
async function get(url:string,token:string){const r=await fetch(url,{headers:{authorization:`Bearer ${token}`},cache:'no-store'});if(r.status===401)redirect('/login');return r.ok?r.json():[]}
export default async function Modules({params}:{params:Promise<{eventId:string}>}){const token=(await cookies()).get('eventise_session')?.value;if(!token)redirect('/login');const base=`${process.env.API_INTERNAL_URL}/api`,organizations=await get(`${base}/organizations`,token)as Array<{id:string}>;if(!organizations.length)redirect('/onboarding');const{eventId}=await params,organizationId=organizations[0].id;const[groups,games,assessments,feedback,features,roster,comparison]=await Promise.all([get(`${base}/organizations/${organizationId}/events/${eventId}/groups`,token),get(`${base}/organizations/${organizationId}/events/${eventId}/games`,token),get(`${base}/organizations/${organizationId}/events/${eventId}/assessments`,token),get(`${base}/organizations/${organizationId}/events/${eventId}/feedback`,token),get(`${base}/organizations/${organizationId}/events/${eventId}/features`,token),get(`${base}/organizations/${organizationId}/events/${eventId}/check-in/roster`,token),get(`${base}/organizations/${organizationId}/events/${eventId}/assessments/comparison`,token)]);return <main className="builder-shell"><header><Link href={`/dashboard/events/${eventId}`}>← Başvurular</Link><div><p className="eyebrow">ETKİNLİK İÇİ</p><h1>Modül merkezi</h1></div></header><ModuleManager organizationId={organizationId} eventId={eventId} initialGroups={groups} initialGames={games} initialAssessments={assessments} initialFeedback={feedback} initialFeatures={features} roster={roster} initialComparison={comparison}/></main>}
