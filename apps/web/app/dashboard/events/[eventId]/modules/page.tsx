import Link from'next/link';import{cookies}from'next/headers';import{redirect}from'next/navigation';import{ModuleManager}from'./module-manager';export default async function Modules({params}:{params:Promise<{eventId:string}>}){const token=(await cookies()).get('eventise_session')?.value;if(!token)redirect('/login');const orgs=await fetch(`${process.env.API_INTERNAL_URL}/api/organizations`,{headers:{authorization:`Bearer ${token}`},cache:'no-store'}).then(r=>r.json())as Array<{id:string}>;if(!orgs.length)redirect('/onboarding');const{eventId}=await params;return <main className="builder-shell"><header><Link href={`/dashboard/events/${eventId}`}>← Başvurular</Link><div><p className="eyebrow">ETKİNLİK İÇİ</p><h1>Modül merkezi</h1></div></header><ModuleManager organizationId={orgs[0].id}eventId={eventId}/></main>}
