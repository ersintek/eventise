import Link from 'next/link';import{cookies}from'next/headers';import{redirect}from'next/navigation';import{LogoutButton}from'./logout-button';interface Organization{id:string;name:string;slug:string;tier:{name:string};memberships:Array<{role:string}>}interface Event{id:string;title:string;slug:string;startsAt:string;publicationStatus:string;registrationStatus:string;_count:{registrations:number}}async function api<T>(path:string,token:string):Promise<T>{const r=await fetch(`${process.env.API_INTERNAL_URL}/api/${path}`,{headers:{authorization:`Bearer ${token}`},cache:'no-store'});if(r.status===401)redirect('/login');if(!r.ok)throw new Error('Veriler alınamadı.');return r.json()}export default async function Dashboard(){const token=(await cookies()).get('eventise_session')?.value;if(!token)redirect('/login');const organizations=await api<Organization[]>('organizations',token);if(!organizations.length)redirect('/onboarding');const org=organizations[0],events=await api<Event[]>(`organizations/${org.id}/events`,token);return <main className="app-shell"><aside className="app-nav"><div className="logo"><b>e</b>eventise</div><div className="org-chip"><span>{org.name.slice(0,2).toUpperCase()}</span><div><b>{org.name}</b><small>{org.memberships[0]?.role.replaceAll('_',' ')}</small></div></div><nav><Link className="active"href="/dashboard">Etkinlikler</Link><Link href="/dashboard/events/new">Yeni etkinlik</Link></nav><LogoutButton/></aside><section className="dashboard"><header><div><p className="eyebrow">KURUM PANELİ · AŞAMA 2</p><h1>Etkinlikler</h1><p>Yayın, kayıt ve başvuru süreçlerini tek yerden yönetin.</p></div><Link className="primary link-button"href="/dashboard/events/new">Yeni etkinlik</Link></header>{events.length===0?<section className="empty-state"><h2>İlk etkinliğinizi oluşturun</h2><p>Duyuru metnini yapıştırarak taslak çıkarabilir veya alanları kendiniz doldurabilirsiniz.</p></section>:<div className="event-grid">{events.map(e=><article key={e.id}><div><span className={`pill ${e.publicationStatus.toLowerCase()}`}>{e.publicationStatus}</span><span className="pill">{e.registrationStatus}</span></div><h2>{e.title}</h2><p>{new Intl.DateTimeFormat('tr-TR',{dateStyle:'long',timeStyle:'short'}).format(new Date(e.startsAt))}</p><b>{e._count.registrations} başvuru</b><Link href={`/dashboard/events/${e.id}`}>Başvuruları yönet →</Link>{e.publicationStatus==='PUBLISHED'&&<Link href={`/events/${org.slug}/${e.slug}`}>Etkinlik sayfasını aç →</Link>}</article>)}</div>}</section></main>}
