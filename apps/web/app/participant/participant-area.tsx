'use client';import{FormEvent,useState}from'react';type History={id:string;title:string;startsAt:string;organization:{name:string}};type Certificate={id:string;verificationCode:string;downloadUrl:string;event:{title:string;organization:{name:string}}};
export function ParticipantArea({history,certificates}:{history:History[];certificates:Certificate[]}){const[message,setMessage]=useState('');async function upload(e:FormEvent<HTMLFormElement>,eventId:string){e.preventDefault();const form=e.currentTarget,fd=new FormData(form),file=fd.get('photo')as File;if(!file)return;const gr=await fetch(`/api/backend/participant/events/${eventId}/photos/upload`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:file.name,contentType:file.type,sizeBytes:file.size})}),grant=await gr.json();if(!gr.ok){setMessage(grant.message??'Yükleme başlatılamadı.');return}const uploaded=await fetch(String(grant.uploadUrl).replace('/api/','/api/backend/'),{method:'PUT',headers:{'content-type':file.type},body:file});if(!uploaded.ok){setMessage('Dosya yüklenemedi.');return}const confirmed=await fetch(`/api/backend/participant/events/${eventId}/photos/confirm`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({assetId:grant.assetId,reservationId:grant.reservationId,caption:fd.get('caption')})});setMessage(confirmed.ok?'Fotoğraf moderasyona gönderildi.':(await confirmed.json()).message)}return <><section className="module-grid">{history.map(event=><article key={event.id}><p className="eyebrow">{event.organization.name}</p><h2>{event.title}</h2><p>{new Intl.DateTimeFormat('tr-TR',{dateStyle:'long'}).format(new Date(event.startsAt))}</p><form onSubmit={e=>upload(e,event.id)}><label>Fotoğraf<input name="photo"type="file"accept="image/jpeg,image/png,image/webp"required/></label><label>Açıklama<input name="caption"/></label><button className="secondary">Moderasyona gönder</button></form></article>)}</section><section><h2>Sertifikalarım</h2>{certificates.length===0?<p>Henüz sertifikanız yok.</p>:certificates.map(c=><article key={c.id}><b>{c.event.title}</b><p>{c.event.organization.name}</p><a href={c.downloadUrl}>PDF indir</a> · <a href={`/certificates/${c.verificationCode}`}>Doğrula</a></article>)}</section>{message&&<p className="notice">{message}</p>}</>}
