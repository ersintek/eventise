'use client';import{FormEvent,useState}from'react';export function AccountSetupForm({token}:{token:string}){const[message,setMessage]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const password=String(new FormData(e.currentTarget).get('password')),r=await fetch('/api/account-setup',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token,password})});setMessage(r.ok?'Hesabınız hazır. Şimdi giriş yapabilirsiniz.':(await r.json()).message)}return <form className="onboarding-card"onSubmit={submit}><div className="logo dark"><b>e</b>eventise</div><h1>Hesabınızı tamamlayın</h1><label>Yeni şifre<input name="password"type="password"minLength={10}required/></label><button className="primary">Şifremi belirle</button>{message&&<p className="notice">{message}</p>}</form>}
