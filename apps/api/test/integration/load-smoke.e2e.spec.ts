import{INestApplication}from'@nestjs/common';import{Test}from'@nestjs/testing';import type{AddressInfo}from'node:net';import{afterAll,beforeAll,describe,expect,inject,it}from'vitest';import{AppModule}from'../../dist/app.module';
describe('Load smoke test',()=>{let app:INestApplication,baseUrl:string;beforeAll(async()=>{process.env.DATABASE_URL=inject('databaseUrl');process.env.JWT_SECRET='integration-test-secret-at-least-32-characters';const module=await Test.createTestingModule({imports:[AppModule]}).compile();app=module.createNestApplication();app.setGlobalPrefix('api');await app.listen(0,'127.0.0.1');const address=app.getHttpServer().address()as AddressInfo;baseUrl=`http://127.0.0.1:${address.port}`});afterAll(()=>app.close());it('serves 100 concurrent health checks without errors',async()=>{const started=Date.now(),responses=await Promise.all(Array.from({length:100},()=>fetch(`${baseUrl}/api/health`)));expect(responses.every(r=>r.status===200)).toBe(true);expect(Date.now()-started).toBeLessThan(10_000)})});
