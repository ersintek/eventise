import{Controller,Get,Inject,Param,Post}from'@nestjs/common';import{AuthenticatedUser,CurrentUser}from'../identity/policies/current-user.decorator';import{ReportingService}from'./reporting.service';@Controller('organizations/:organizationId')export class ReportingController{constructor(@Inject(ReportingService)private r:ReportingService){}@Get('events/:eventId/reports/summary')summary(@CurrentUser()u:AuthenticatedUser,@Param('organizationId')o:string,@Param('eventId')e:string){return this.r.summary(u.id,o,e)}@Post('events/:eventId/reports/:format')request(@CurrentUser()u:AuthenticatedUser,@Param('organizationId')o:string,@Param('eventId')e:string,@Param('format')f:'csv'|'xlsx'|'pdf'){return this.r.request(u.id,o,e,f)}@Get('reports/:id')status(@CurrentUser()u:AuthenticatedUser,@Param('organizationId')o:string,@Param('id')id:string){return this.r.status(u.id,o,id)}}
