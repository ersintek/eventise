import{describe,expect,it}from'vitest';import{featureRegistry,findFeature}from'./feature-registry';describe('feature registry',()=>it('contains unique modular event tools without phase restrictions',()=>{expect(new Set(featureRegistry.map(f=>f.key)).size).toBe(featureRegistry.length);expect(findFeature('door_registration')).toMatchObject({key:'door_registration'})}));
