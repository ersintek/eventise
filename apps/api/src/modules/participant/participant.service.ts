import{Inject,Injectable,NotFoundException}from'@nestjs/common';import{PrismaService}from'../../shared/persistence/prisma.service';@Injectable()export class ParticipantService{constructor(@Inject(PrismaService)private prisma:PrismaService){}async modules(userId:string,eventId:string){const user=await this.prisma.user.findUnique({where:{id:userId}}),registration=user?await this.prisma.eventRegistration.findUnique({where:{eventId_email:{eventId,email:user.email}}}):null;if(!registration||registration.applicationStatus!=='ACCEPTED')throw new NotFoundException('Kabul edilmiş katılımcı kaydı bulunamadı.');const[assessments,feedback,games,resources,notifications]=await Promise.all([this.prisma.assessment.findMany({where:{eventId,open:true},select:{id:true,kind:true,title:true,schema:true,submissions:{where:{registrationId:registration.id},select:{id:true,score:true}}}}),this.prisma.feedbackForm.findMany({where:{eventId,open:true},select:{id:true,title:true,schema:true,submissions:{where:{registrationId:registration.id},select:{id:true}}}}),this.prisma.gameSession.findMany({where:{eventId,status:'OPEN'},select:{id:true,title:true,config:true,assignments:{where:{registrationId:registration.id}},responses:{where:{registrationId:registration.id}}}}),this.prisma.eventResource.findMany({where:{eventId,visible:true},select:{id:true,title:true,kind:true,externalUrl:true}}),this.prisma.inAppNotification.findMany({where:{userId,eventId},orderBy:{createdAt:'desc'},take:20})]);return{eventId,assessments,feedback,games,resources,notifications}}}
