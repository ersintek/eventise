import{Inject,Injectable}from'@nestjs/common';import{PrismaService}from'../../shared/persistence/prisma.service';import{OrganizationAccessService}from'../organizations/policies/organization-access.service';import{AuditService}from'../audit/audit.service';
@Injectable()export class NotificationsService{constructor(@Inject(PrismaService)private prisma:PrismaService,@Inject(OrganizationAccessService)private access:OrganizationAccessService,@Inject(AuditService)private audit:AuditService){}async broadcast(userId:string,organizationId:string,eventId:string,title:string,body:string){await this.access.requireEventAccess(userId,organizationId,eventId,['ORGANIZATION_ADMIN','EVENT_MANAGER']);const users=await this.prisma.eventRegistration.findMany({where:{eventId,applicationStatus:'ACCEPTED',userId:{not:null}},select:{userId:true}}),ids=[...new Set(users.flatMap(r=>r.userId?[r.userId]:[]))];if(ids.length)await this.prisma.inAppNotification.createMany({data:ids.map(id=>({userId:id,eventId,title,body}))});await this.audit.record({actorId:userId,organizationId,action:'notification.broadcast',resourceType:'event',resourceId:eventId,metadata:{recipientCount:ids.length}});return{recipientCount:ids.length}}list(userId:string){return this.prisma.inAppNotification.findMany({where:{userId},orderBy:{createdAt:'desc'},take:100})}async read(userId:string,id:string){const updated=await this.prisma.inAppNotification.updateMany({where:{id,userId},data:{readAt:new Date()}});return{updated:updated.count}}}
