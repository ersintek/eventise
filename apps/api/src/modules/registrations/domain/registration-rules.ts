import { BadRequestException } from '@nestjs/common'; import { Event } from '@prisma/client';
export function initialRegistrationStatus(event:Pick<Event,'registrationMode'|'capacity'>,acceptedCount:number){if(event.registrationMode==='APPROVAL')return'PENDING';if(acceptedCount>=event.capacity)return'WAITLISTED';return event.registrationMode==='WAITLIST'?'WAITLISTED':'ACCEPTED'}
export function assertRegistrationOpen(event:Pick<Event,'publicationStatus'|'registrationStatus'|'registrationOpensAt'|'registrationClosesAt'>,now=new Date()){if(event.publicationStatus!=='PUBLISHED'||event.registrationStatus!=='OPEN'||(event.registrationOpensAt&&now<event.registrationOpensAt)||(event.registrationClosesAt&&now>event.registrationClosesAt))throw new BadRequestException('Etkinlik kayıt kabul etmiyor.');}
export const allowedRegistrationTransitions:Record<string,string[]>={SUBMITTED:['PENDING','ACCEPTED','WAITLISTED','REJECTED'],PENDING:['ACCEPTED','WAITLISTED','REJECTED'],WAITLISTED:['ACCEPTED','REJECTED'],ACCEPTED:['REJECTED'],REJECTED:[]};
