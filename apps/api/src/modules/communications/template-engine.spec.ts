import { describe, expect, it } from 'vitest'; import { emailVariables, renderTemplate, validateTemplate } from './template-engine';
describe('email template engine',()=>{it('rejects unsupported variables and unsafe html',()=>{expect(()=>validateTemplate('{{unknown.value}}',emailVariables)).toThrow();expect(()=>validateTemplate('<script>alert(1)</script>',emailVariables)).toThrow()});it('fails closed on missing variables and escapes values',()=>{expect(()=>renderTemplate('{{event.name}}',{})).toThrow();expect(renderTemplate('{{event.name}}',{'event.name':'<Buluşma>'})).toBe('&lt;Buluşma&gt;')})});
