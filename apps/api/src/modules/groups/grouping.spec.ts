import{describe,expect,it}from'vitest';import{distributeGroups}from'./grouping';describe('group distribution',()=>{it('keeps random group sizes within one',()=>{const g=distributeGroups(Array.from({length:10},(_,i)=>({id:String(i)})),3,'RANDOM',()=>.4);expect(Math.max(...g.map(x=>x.length))-Math.min(...g.map(x=>x.length))).toBeLessThanOrEqual(1)});it('spreads balanced values across groups',()=>{const g=distributeGroups([{id:'a',balanceValue:'x'},{id:'b',balanceValue:'x'},{id:'c',balanceValue:'y'},{id:'d',balanceValue:'y'}],2,'BALANCED');expect(g).toEqual([['a','c'],['b','d']])})});
