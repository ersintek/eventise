export interface UploadGrant { key: string; uploadUrl: string; expiresAt: Date; }
export abstract class StorageProvider { abstract createUploadGrant(key: string, contentType: string, expiresInSeconds: number): Promise<UploadGrant>; abstract createDownloadUrl(key: string, expiresInSeconds: number): Promise<string>; abstract put(key:string,data:Buffer,contentType:string):Promise<void>; abstract delete(key: string): Promise<void>; abstract get(key: string): Promise<Buffer | null>; }
